<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="claim_table">
	
	<insert id="claimInsert" parameterType="claimvo">
		insert into claim_table(user_idx, user_id, claim_subject, claim_content, claim_reply, claim_pw_status, claim_pw, claim_reply_date, claim_created_date)
		values(#{user_idx},#{user_id},#{claim_subject},#{claim_content},#{claim_reply},#{claim_pw_status},#{claim_pw}, sysdate(), sysdate())
	</insert>
	
	
</mapper>















